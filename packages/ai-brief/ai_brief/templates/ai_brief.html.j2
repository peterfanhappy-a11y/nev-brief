<!DOCTYPE html>
<html lang="zh-CN"><head><meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta name="color-scheme" content="light">
<title>{{ brief.subject }}</title>
</head>
<body style="margin:0;padding:0;background:#f2f2f2;font-family:-apple-system,'PingFang SC','Microsoft YaHei',Helvetica,Arial,sans-serif;">
<!-- preheader（隐藏预览文字）-->
<div style="display:none;max-height:0;overflow:hidden;opacity:0;">{{ brief.preheader }}</div>

{% macro divider() %}
<tr><td style="padding:0 28px;"><div style="border-top:1px solid #e5e7eb;height:1px;line-height:1px;font-size:1px;">&nbsp;</div></td></tr>
{% endmacro %}

{% macro section_header(title) %}
<tr><td style="padding:26px 28px 6px;">
  <div style="background:#111827;color:#ffffff;font-size:14px;font-weight:700;letter-spacing:3px;padding:9px 0;text-align:center;border-radius:6px;">{{ title }}</div>
</td></tr>
{% endmacro %}

<table role="presentation" width="100%" cellpadding="0" cellspacing="0" style="background:#f2f2f2;">
<tr><td align="center" style="padding:20px 12px;">
<table role="presentation" width="600" cellpadding="0" cellspacing="0" style="width:600px;max-width:600px;background:#ffffff;">

  <!-- ① Header：logo 居中 + 日期/阅读时长（无社交图标）-->
  <tr><td style="padding:32px 28px 20px;text-align:center;">
    <img src="{{ logo_email_url }}" height="34" alt="AIVIZENS 趋势" style="display:block;margin:0 auto;">
    <div style="font-size:11px;color:#9ca3af;margin-top:8px;letter-spacing:1px;">Together with 不凡的数智生活</div>
    <div style="font-size:12px;color:#9ca3af;margin-top:10px;letter-spacing:1px;">{{ date_human }} · 5 分钟阅读</div>
  </td></tr>

  {{ divider() }}

  <!-- ② 欢迎 + 作者 -->
  <tr><td style="padding:22px 28px 6px;">
    <table role="presentation" cellpadding="0" cellspacing="0"><tr>
      <td style="vertical-align:middle;"><img src="{{ author_photo_url }}" width="38" height="38" alt="{{ author_name }}" style="border-radius:50%;display:block;"></td>
      <td style="vertical-align:middle;padding-left:10px;font-size:14px;color:#374151;font-weight:600;">{{ author_name }}</td>
    </tr></table>
    <p style="margin:16px 0 4px;font-size:15px;line-height:1.75;color:#374151;">
      欢迎回来！以下是今天最值得关注的 AI 洞察，5 分钟带你看懂全局。
    </p>
  </td></tr>

  <!-- ③ 今日 AI 简报 bullets -->
  <tr><td style="padding:8px 28px 18px;">
    <div style="font-size:12px;font-weight:700;color:#6b7280;letter-spacing:2px;margin-bottom:10px;">今日 AI 简报</div>
    {% for b in brief.intro_bullets %}
    <div style="font-size:14px;line-height:1.9;color:#374151;">{{ b }}</div>
    {% endfor %}
  </td></tr>

  <!-- ④ 今日精选（窄横幅 + 图文，item 之间分割线）-->
  {{ section_header('今 日 精 选') }}
  {% for item in brief.featured %}
  {% if not loop.first %}{{ divider() }}{% endif %}
  <tr><td style="padding:20px 28px;">
    <div style="font-size:11px;font-weight:700;color:{{ theme_color(item.theme) }};letter-spacing:2px;margin-bottom:10px;">{{ item.theme_label | upper }}</div>
    {% if item.og_image %}
    <img src="{{ item.og_image }}" width="544" alt="" style="display:block;width:100%;height:auto;border-radius:8px;margin-bottom:14px;">
    {% endif %}
    <a href="{{ item.url }}" style="text-decoration:none;"><div style="font-size:19px;font-weight:800;color:#111827;line-height:1.4;margin-bottom:12px;">{{ item.headline }}</div></a>
    <div style="font-size:13px;font-weight:700;color:#111827;margin-bottom:6px;">详情：</div>
    <ul style="margin:0 0 14px;padding-left:20px;">
      {% for d in item.details %}
      <li style="font-size:14px;line-height:1.75;color:#374151;margin-bottom:3px;">{{ d }}</li>
      {% endfor %}
    </ul>
    <div style="font-size:14px;line-height:1.75;color:#374151;">
      <span style="font-weight:700;color:#111827;">为什么重要：</span>{{ item.significance }}
    </div>
    <div style="margin-top:12px;"><a href="{{ item.url }}" style="font-size:13px;color:#4F46E5;text-decoration:none;font-weight:600;">阅读原文（{{ item.source_name }}）→</a></div>
  </td></tr>
  {% endfor %}

  {{ divider() }}

  <!-- ⑤ 今日速览（窄横幅）-->
  {{ section_header('今 日 速 览') }}
  {% if brief.tools %}
  <tr><td style="padding:18px 28px 6px;">
    <div style="font-size:14px;font-weight:800;color:#111827;margin-bottom:10px;">🔥 热门 AI 工具</div>
    {% for t in brief.tools %}
    <div style="font-size:14px;line-height:1.7;color:#374151;margin-bottom:6px;">
      <a href="{{ t.url }}" style="color:#111827;font-weight:700;text-decoration:none;">{{ t.name }}</a> — {{ t.one_liner }}
    </div>
    {% endfor %}
  </td></tr>
  {% endif %}
  {% if brief.daily_tip %}
  {% if brief.tools %}{{ divider() }}{% endif %}
  <tr><td style="padding:16px 28px;">
    <div style="font-size:14px;font-weight:800;color:#111827;margin-bottom:8px;">🎓 每日课堂 · {{ brief.daily_tip.title }}</div>
    <div style="font-size:14px;line-height:1.75;color:#374151;">{{ brief.daily_tip.body }}</div>
  </td></tr>
  {% endif %}
  {% if brief.quick_hits %}
  {{ divider() }}
  <tr><td style="padding:16px 28px;">
    <div style="font-size:14px;font-weight:800;color:#111827;margin-bottom:8px;">📰 AI 其他</div>
    {% for q in brief.quick_hits %}
    <div style="font-size:14px;line-height:1.8;color:#374151;margin-bottom:4px;">
      • {% if q.url %}<a href="{{ q.url }}" style="color:#374151;text-decoration:none;">{{ q.text }}</a>{% else %}{{ q.text }}{% endif %}
    </div>
    {% endfor %}
  </td></tr>
  {% endif %}

  <!-- ⑥ 昨日最热 -->
  {% if brief.yesterday_top %}
  {{ divider() }}
  <tr><td style="padding:18px 28px;">
    <span style="font-size:13px;font-weight:700;color:#6b7280;">如果您错过了：</span>
    <a href="{{ brief.yesterday_top.url }}" style="font-size:14px;color:#111827;text-decoration:none;font-weight:600;">{{ brief.yesterday_top.headline }}</a>
  </td></tr>
  {% endif %}

  {{ divider() }}

  <!-- ⑦ 评分 -->
  <tr><td style="padding:24px 28px;text-align:center;">
    <div style="font-size:14px;font-weight:700;color:#374151;margin-bottom:14px;">今天的内容怎么样？</div>
    <a href="{{ rate_base }}?d={{ delivery_id }}&amp;s=3" style="text-decoration:none;font-size:26px;padding:0 12px;">🤩</a>
    <a href="{{ rate_base }}?d={{ delivery_id }}&amp;s=2" style="text-decoration:none;font-size:26px;padding:0 12px;">🙂</a>
    <a href="{{ rate_base }}?d={{ delivery_id }}&amp;s=1" style="text-decoration:none;font-size:26px;padding:0 12px;">😐</a>
    <div style="font-size:11px;color:#9ca3af;margin-top:10px;">很棒 · 还行 · 一般</div>
  </td></tr>

  <!-- ⑧ Footer：社交图标 + 退订 -->
  <tr><td style="padding:24px 28px 30px;background:#111827;text-align:center;">
    <img src="{{ logo_email_url }}" height="24" alt="AIVIZENS" style="display:block;margin:0 auto 14px;filter:brightness(0) invert(1);">
    <div style="margin-bottom:14px;">
      {% for name, icon_url, href in social_icons %}
      <a href="{{ href }}" style="text-decoration:none;padding:0 6px;"><img src="{{ icon_url }}" width="22" height="22" alt="{{ name }}" style="border-radius:4px;vertical-align:middle;"></a>
      {% endfor %}
    </div>
    <div style="font-size:12px;color:#9ca3af;line-height:1.7;">
      由 AIVIZENS 出品 · 每日 5 分钟看懂 AI<br>
      <a href="{{ unsubscribe_url }}" style="color:#9ca3af;">一键退订</a> ·
      <a href="{{ web_base }}" style="color:#9ca3af;">访问网站</a>
    </div>
  </td></tr>

</table>
</td></tr></table>
</body></html>
