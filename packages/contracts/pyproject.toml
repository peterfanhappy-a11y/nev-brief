[project]
name = "nev-contracts"
version = "0.1.0"
description = "共享数据模型与枚举"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.7",
    "email-validator>=2.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["python"]
